// `semi: false` is the front-end-cursor-rules no-semicolon requirement. It is
// configuration rather than review guidance so it cannot be discovered late.
const config = {
  semi: false,
  singleQuote: false,
  trailingComma: "all",
  printWidth: 88,
  arrowParens: "always",
}

export default config
