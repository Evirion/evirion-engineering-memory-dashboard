[project]
name = "evirion-console-security-tools"
version = "0.0.0"
description = "Digest-pinned static analysis toolchain for the Design Partner Console."
requires-python = ">=3.11,<3.14"
dependencies = ["semgrep==1.176.0"]

[tool.uv]
package = false
