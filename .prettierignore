# Digest-pinned authority. CI reproduces these bytes and fails on any diff, so
# a formatter must never rewrite them. `docs/` is excluded wholesale because it
# contains frozen manifest members, including the controlling EEM-9 plan, whose
# bytes may only change through a reviewed successor pointer.
docs/
generated/
vendor/

.next/
.venv/
coverage/
node_modules/
playwright-report/
test-results/
pnpm-lock.yaml
tools/local-tls/.local/
tools/security/.local/
tools/security/.venv/

# Generated by Next.js and explicitly marked "should not be edited".
next-env.d.ts
